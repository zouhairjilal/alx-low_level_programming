#include "main.h"

/**
 * _isalpha - a function that checks for alphabetic character,
 * uppercase or lower case
 * @c: is an argument
 * Return: 0
 */

int _isalpha(int c)
{
	return (c >= 'a' || c >= 'A');
}
