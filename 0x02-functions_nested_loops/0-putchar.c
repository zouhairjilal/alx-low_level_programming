#include <stdio.h>
#include <main.h>

/**
 * main - a program that prints _putchar, followed by a new line.
 * Return: if return is 0 that's a good sign.
 */

int main(void)
{
	printf("_putchar\n");
	return(0);
}
